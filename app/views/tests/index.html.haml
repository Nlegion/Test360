%h1 Список тестов
%br
- if @tests.any?
  %table{:class => 'table table-striped table-bordered'}
    %tr
      %th{:class => 'text-center'} Название
      %th{:class => 'text-center'} Описание
      %th{:class => 'text-center'} Запуск            
      %th{:class => 'text-center'} Действия
    - @tests.each do |t| 
      %tr
        %td
          = t.name
        %td 
          = t.description
        %td{:class => 'text-center'}
          = link_to 'Пройти тест', get_answ_test_answers_path(t), class: "btn btn-success btn-xs" 
        %td{:class => 'text-center'} 
          = link_to 'Подробнее', test_path(t), class: "btn btn-info btn-xs"
          = "   "
          = link_to 'Редактировать', edit_test_path(t), class: "btn btn-warning btn-xs"
          = "   "
          = link_to 'Удалить', test_path(t), class: "btn btn-danger btn-xs", method: :delete, data: { confirm: 'Уверен?' }
%br
= link_to 'Создать новый тест', new_test_path, class: "btn btn-default btn" 


            